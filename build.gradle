apply plugin: 'java-library'
apply plugin: 'application'

mainClassName = "com.TradeDesk.util"

repositories {
    jcenter()
}

dependencies {
	compile group: 'com.google.code.gson', name: 'gson', version: '2.7'
}

